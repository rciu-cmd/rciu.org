import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Join Our Club",
  description: "Interested in joining Rotary Club of Ikh Urgoo? Here's how to become a member, and a form to get in touch.",
};

export default function JoinLayout({ children }: { children: React.ReactNode }) {
  return children;
}
